# 보안 정책

AI School OS는 학교 과제 관리 흐름을 검증하는 MVP입니다. 운영 서비스에 적용하기 전에는 학교·학생 데이터에 맞는 위협 모델, 개인정보 보관 정책, 계정 복구와 승인 절차를 별도로 마련해야 합니다.

## 지원 범위

보안 수정은 기본 브랜치의 최신 상태를 기준으로 검토합니다. 오래된 커밋이나 포크에서만 재현되는 문제는 재현에 필요한 커밋과 환경을 함께 알려 주세요.

## 취약점 신고

취약점은 공개 이슈나 공개 PR로 올리지 마세요. 저장소의 GitHub **비공개 취약점 신고(Private vulnerability reporting)** 또는 **Security Advisory** 기능을 사용해 저장소 관리자에게 직접 신고해 주세요. 해당 기능이 보이지 않으면 저장소 소유자의 GitHub 프로필을 통해 비공개 연락을 요청하세요.

신고에는 다음 내용을 포함하면 triage에 도움이 됩니다.

- 영향을 받는 커밋, 경로, 버전
- 재현 단계와 필요한 환경변수 또는 데이터
- 공격자가 얻을 수 있는 영향과 필요한 권한
- 재현 로그·스크린샷·최소 재현 코드
- 이미 알고 있는 완화 방법

민감한 토큰, 실제 학생 정보, 외부 AI API 키는 첨부하지 말고 모두 가린 뒤 공유하세요.

## 운영 전 점검

- `AUTH_SECRET`을 배포마다 안전한 32바이트 이상 무작위 값으로 관리하고, `.env`와 API 키를 저장소에 커밋하지 않습니다.
- HTTPS와 Secure 쿠키를 사용하고, 배포 환경의 `APP_URL`, `TRUST_PROXY`, `SERVER_ACTION_ALLOWED_ORIGINS`를 실제 주소와 프록시 구성에 맞게 설정합니다.
- 교사 가입은 `TEACHER_INVITE_CODE`가 설정되고 코드가 일치할 때만 허용됩니다. 기존 또는 별도 심사 계정은 DB 운영자가 `npm run teacher:approve -- email`로 승인할 수 있으며, 장기 운영에서는 학교 도메인·이메일 인증으로 교체하세요.
- 첨부파일은 파일당 5MiB, 클래스별 50개·50MiB로 제한합니다. 악성코드 검사, 오브젝트 스토리지, 백업과 보존 기간 정책은 운영 전에 추가해야 합니다.
- AI는 학생의 명시적 동의 후 학년·반과 과제 문맥만 전달하며, 설정에서 동의 철회와 대화 기록 삭제를 제공합니다. 외부 제공자의 보관·지역·학습 사용 정책은 별도 계약과 설정이 필요합니다.
- 인증·클래스 참여·AI 요청에 프록시 IP 및 계정별 제한을 적용하고, 실패한 로그인은 이메일·IP 조합별로 짧게 제한합니다. 운영에서는 프록시 계층의 실제 IP 전달을 검증하고 모니터링·감사 로그·장애 시 복구 절차를 운영하세요.
- `TRUST_PROXY=false`인 직접 공개 구성에서는 모든 요청이 `direct` 버킷을 공유하므로, 공개 운영은 프록시가 외부 IP 헤더를 정리하는 환경에서만 활성화하세요.
- 과제 삭제는 제출물과 AI 기록을 보존하는 보관 처리입니다. 관리자용 영구 파기와 복구 가능한 백업 정책은 별도로 설계해야 합니다.

현재 구현된 보호 장치와 알려진 제한은 [README의 보안과 운영 범위](README.md#보안과-운영-범위)에 정리되어 있습니다.
